/**
 * A helper class for reduce process.
 */
export class Reducer {
    static add(x: number, y: number): number {
        return x + y
    }

    static strcat(x: string, y: string): string {
        return x + y;
    }
}
